#!/usr/bin/env bash
# Setup script for neat-little-package development environment

set -e

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

echo "Setting up neat-little-package..."

# Git hooks
ln -sf ../../helpers/pre-commit .git/hooks/pre-commit
echo "  ✓ pre-commit hook installed"

echo "Done."
